{
  "main": "data.json",
  "scripts": {
    "server":"json-server --watch data.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ainhoa Aguado",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}